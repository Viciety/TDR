/*
describe('Score Chooser Spec', function() {

    describe("", function() {

        let taulell1 = new DeepScore(new SpecScore(5), 1);
        let taulell2 = new DeepScore(new SpecScore(5), 2);
        let taulell3 = new DeepScore(new SpecScore(15), 2);
        let taulell4 = new DeepScore(new SpecScore(10), 3);


        it('Deep Score 5 i 1', function() {
            expect(taulell1.compare(score1)).toEqual(0);
            expect(taulell1.compare(score2)).toEqual(-1);
            expect(taulell1.compare(score3)).toEqual(-10);
            expect(taulell1.compare(score4)).toEqual(-5);
        });

    });    
});
*/